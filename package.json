{
  "name": "usagecheck-gpt",
  "version": "0.1.0",
  "private": true,
  "description": "Central ChatGPT usage counter with browser collector and aggregate dashboard",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
